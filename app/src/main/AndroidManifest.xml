<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.karvan">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".DeactivateTwoActivity"></activity>
        <activity android:name=".DeactivateOneActivity" />
        <activity android:name=".ViewRequestsActivity" />
        <activity android:name=".SearchResultsActivity" />
        <activity android:name=".SelectOneServiceActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".ViewingOtherProfileActivity" />
        <activity android:name=".InputPreferencesAndInformationActivity" />
        <activity android:name=".OfferingSkillsSelectionActivity" />
        <activity android:name=".AccountDetailsActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".StartUpActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>